#include <iostream>
#include <stdio.h>
#include <string>
#include <vector>

using namespace std;

int solution(vector<int> d, int budget) {

    for (int i=0; i<d.size();i++){
        budget=budget-d[i];
            if(budget==0)
                return d.size();
    }
